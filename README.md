# cat-detection-using-python-and-opencv2
this program uses python,opencv and a learning xml file to detect cat faces


for this to work you need to install opencv this can be installec with:
pip install opencv-python
